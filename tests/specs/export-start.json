[
  {
    "name": "Export Set starts export flow",
    "steps": [
      {
        "send": { "callback": "export:start" },
        "expect": [
          {
            "method": "answerCallbackQuery"
          },
          {
            "method": "sendMessage",
            "payload": {
              "text": "You have no replies to export.\n\nCreate some replies first."
            }
          }
        ]
      }
    ]
  }
]
