[
  {
    "name": "Add New Reply starts creation flow",
    "steps": [
      {
        "send": { "callback": "reply:add" },
        "expect": [
          {
            "method": "answerCallbackQuery"
          },
          {
            "method": "sendMessage",
            "payload": {
              "text": "What should this reply be called?"
            }
          }
        ]
      }
    ]
  }
]
